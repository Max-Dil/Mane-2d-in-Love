Create:
local world = mane.physics.newWorld() -- newWorld(gx, gy, sleep)

Property:
    world = love.physics.newWorld( gx or 0, gy or 0, sleep and sleep or false),
    update = false -- it is necessary to set it to true so that the world is updated otherwise it sleeps
    events = {
        collision = {},
        endCollision = {},
        preCollision = {},
        postCollision = {}
    }

Methods:
    :addBody(obj, bodyType, options)
